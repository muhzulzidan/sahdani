/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://sahdani.id',
    generateRobotsTxt: true, // (optional) default is true
    sitemapSize: 5000, // (optional) default is 5000
    // ...other options
}